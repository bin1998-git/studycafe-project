package com.tenco.ticket;

import java.util.List;

public class TicketService {
    // 이용권 상품 등록
    public boolean addTicket(TicketDTO ticketDTO) {

    }

    // 전체 이용권 상품 반환
    public List<TicketDTO> getTicketList() {

    }

    // ID로 조회, 없으면 예외
    public TicketDTO getTicketById(int ticketId) {

    }

    // 이용권 상품 수정
    public boolean modifyTicket(TicketDTO ticketDTO) {

    }

    // 이용권 상품 삭제
    public boolean issueTicketToMember(int memberId, int ticketId) {

    }

    // 회원에게 이용권 발급 (memberId, ticketId)
    public List
    // 회원 보유 이용권 목록 조회
}
